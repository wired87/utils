# enable specific ports on vm
# 1. UFW installieren
# 2. UFW aktivieren (falls noch nicht aktiv)
# 3. Port 8001 freigeben
# 4. Status prüfen

sudo apt update
sudo apt install ufw
sudo ufw enable
sudo ufw allow 8001/tcp
sudo ufw reload
sudo ufw status
