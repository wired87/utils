

CONNECT TO EC2 MACHINE
ssh -i <name pem file> <files to upload> sc2-usr@<pubIp>:<work dir>
../extract_data/data/raw/ensembl.json
json_upload.py process_ensembl.py

scp -i HelloGene.pem ../extract_data/functions/file/json_to_jsonl.py ec2-user@54.87.173.92:/home/ec2-user/work


$ scp -i HelloGene.pem process_ensembl.py ec2-user@54.145.236.241:/home/ec2-user/processdir
#
#last ENSG00000299802


MAKE PEM FILE READ ONLY (Before running)
chmod 600 <name pem file>

mkdir work && cd work && python3 -m venv workenv && source workenv/bin/activate && python3 process_ensembl.py
AWS
cd workdir
create venv : python3 -m venv workenv
run venv : source workenv/bin/activate


TMUX (let programs tun after local pc shut down)
sudo yum install tmux
tmux new -s worksession
start venv
python3 your-program.py
Ctrl+B, then D
RECONNECT
tmux attach -t worksession
''agent ol

Copy open s3 uri -> own s3 bucket(golder)
aws s3 cp s3://aws-open-data-uniprot-rdf s3://brainmaster01/data/ --recursive
Access all s3
https://us-east-1.cohttps://us-east-1.console.aws.amazon.com/s3/buckets/brainmaster01?region=us-east-1&bucketType=general&prefix=data/nsole.aws.amazon.com/s3/buckets/brainmaster01?region=us-east-1&bucketType=general&prefix=data/



aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 652939428319.dkr.ecr.us-east-1.amazonaws.com

